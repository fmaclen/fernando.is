export enum Icons {
  X,
  DRIBBBLE,
  GITHUB,
  EMAIL,
  LINKEDIN,
  DARK,
  LIGHT,
  CLOSE,
  PREVIOUS,
  NEXT,
  EXTERNAL_LINK
}

export enum IconModifier {
  THEMABLE = "themable",
  IN_DIALOG = "in-dialog",
  DARK_MODE = "dark-mode",
  LIGHT_MODE = "light-mode"
}
